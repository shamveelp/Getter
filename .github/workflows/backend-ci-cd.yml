name: Getter Backend CI/CD

on:
  push:
    branches:
      - master
    paths:
      - "getter-backend/**"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        working-directory: getter-backend
        run: npm install

      - name: Build backend
        working-directory: getter-backend
        run: npm run build

      - name: Copy backend to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "getter-backend/dist,getter-backend/package.json"
          target: "/home/ubuntu/apps/getter-backend"

      - name: Restart backend on EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd /home/ubuntu/apps/getter-backend/getter-backend
            npm install --omit=dev
            pm2 restart getter-backend || pm2 start dist/index.js --name getter-backend
            pm2 save